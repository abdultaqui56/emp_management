package com.employee.management.bandLevel.repository;

import com.employee.management.bandLevel.model.BandLevel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BandLevelRepository extends JpaRepository<BandLevel, Long> {
}
